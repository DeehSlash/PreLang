@add ($a: int, $b: int) -> int {
  return $a + $b
}

@sub ($a: int, $b: int) -> int {
  return $a - $b
}

@main ($arg: char) -> int {
  if($arg == '+') {
    return @add(1, 1)
  } else {
    return @sub(1, 1)
  }
}